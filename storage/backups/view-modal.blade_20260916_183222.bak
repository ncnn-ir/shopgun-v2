<div>
    @if($show && $order)
    <div class="fixed inset-0 z-[85] flex items-start justify-center p-4 overflow-y-auto"
         x-data="{ show: true }" x-init="$nextTick(() => show = true)"
         @keydown.escape.window="$wire.close()">

        <div class="fixed inset-0 bg-black/60 backdrop-blur-md"
             x-show="show"
             x-transition:enter="ease-out duration-300"
             x-transition:enter-start="opacity-0"
             x-transition:enter-end="opacity-100"
             wire:click="close"></div>

        <div class="relative bg-base-100 rounded-2xl shadow-2xl w-full max-w-3xl my-8 md:my-16 border border-base-300"
             x-show="show"
             x-transition:enter="ease-out duration-300"
             x-transition:enter-start="opacity-0 translate-y-8 scale-95"
             x-transition:enter-end="opacity-100 translate-y-0 scale-100">

            <div class="flex items-center justify-between p-4 border-b border-base-300 bg-gradient-to-l from-primary/10 to-transparent rounded-t-2xl sticky top-0 z-10 bg-base-100/95 backdrop-blur">
                <div class="flex items-center gap-3">
                    <div class="w-9 h-9 rounded-lg bg-gradient-to-br from-cyan-500 to-teal-600 flex items-center justify-center text-white shadow">📋</div>
                    <div>
                        <h2 class="font-bold text-base">سفارش #{{ $order->order_number }}</h2>
                        <div class="text-xs text-base-content/60">{{ $order->created_at?->format('Y/m/d H:i') }}</div>
                    </div>
                </div>
                <button wire:click="close" class="btn btn-ghost btn-sm btn-circle">✕</button>
            </div>

            <div class="p-5 space-y-4 max-h-[calc(100vh-14rem)] overflow-y-auto">

                <div class="grid grid-cols-2 md:grid-cols-3 gap-2">
                    <div class="p-3 rounded-lg bg-base-200/50">
                        <div class="text-[10px] text-base-content/60">👤 مشتری</div>
                        <button onclick="Livewire.dispatch('show-customer-profile', { phone: '{{ $order->phone }}' })"
                                class="font-bold text-sm link link-primary mt-1">{{ $order->customer?->name ?? '—' }}</button>
                    </div>
                    <div class="p-3 rounded-lg bg-base-200/50">
                        <div class="text-[10px] text-base-content/60">📱 تلفن</div>
                        <div class="font-mono text-sm mt-1" dir="ltr">{{ $order->phone ?? '—' }}</div>
                    </div>
                    <div class="p-3 rounded-lg bg-base-200/50">
                        <div class="text-[10px] text-base-content/60">📮 کدپستی</div>
                        <div class="font-mono text-sm mt-1" dir="ltr">{{ $order->postal_code ?? '—' }}</div>
                    </div>
                    <div class="p-3 rounded-lg bg-base-200/50">
                        <div class="text-[10px] text-base-content/60">📊 وضعیت</div>
                        <div class="mt-1"><span class="badge-status {{ $order->status }}">{{ $order->status_label }}</span></div>
                    </div>
                    <div class="p-3 rounded-lg bg-base-200/50">
                        <div class="text-[10px] text-base-content/60">🌐 کانال</div>
                        <div class="mt-1 text-sm">{{ $order->channel?->icon }} {{ $order->channel?->name ?? '—' }}</div>
                    </div>
                    <div class="p-3 rounded-lg bg-base-200/50">
                        <div class="text-[10px] text-base-content/60">💰 بیمه</div>
                        <div class="mt-1 font-bold">{{ $order->insurance ? number_format((float) $order->insurance) : '—' }}</div>
                    </div>
                </div>

                <div class="p-3 rounded-lg bg-base-200/50">
                    <div class="text-[10px] text-base-content/60 mb-1">📍 آدرس</div>
                    <div class="text-sm leading-6">{{ $order->address ?? '—' }}</div>
                </div>

                @if($order->items->isNotEmpty())
                    <div>
                        <div class="font-bold text-sm mb-2">🛍️ محصولات</div>
                        <table class="mini-table">
                            <tbody>
                                @foreach($order->items as $i => $it)
                                    <tr>
                                        <td class="text-xs w-6">{{ $i + 1 }}</td>
                                        <td class="text-xs">{{ $it->title }}</td>
                                        <td class="text-[10px] font-mono" dir="ltr">{{ $it->sku ?? '—' }}</td>
                                        <td class="text-[11px]">{{ number_format((float) $it->price) }}</td>
                                        <td class="text-[11px]">×{{ $it->quantity }}</td>
                                    </tr>
                                @endforeach
                            </tbody>
                        </table>
                    </div>
                @endif

                @if($order->notes)
                    <div class="p-3 rounded-lg bg-warning/10 border border-warning/30">
                        <div class="text-[10px] text-warning-content mb-1">📝 یادداشت</div>
                        <div class="text-sm">{{ $order->notes }}</div>
                    </div>
                @endif
            </div>

            <div class="p-4 border-t border-base-300 flex justify-end gap-2 rounded-b-2xl bg-base-200/50">
                <button wire:click="close" class="btn btn-ghost btn-sm">بستن</button>
                <a href="{{ route('orders.show', $order) }}" class="btn btn-outline btn-sm">صفحه کامل</a>
            </div>
        </div>
    </div>
    @endif
</div>
